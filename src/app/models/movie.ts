export interface Movie {
    id: string;
    title: string;
    duration: number;
    airDate: string;
    imageUrl: string;
    image?: string;
    base64?: string;
    path: string;
    subs: string;
}
